# Copy this file to .env (or .env.local for `vercel dev`) and fill in real
# values. Never commit the real .env file — it's already in .gitignore.
#
# Still used by /api/submit-form for the "Partner application" form
# (partners.html) only. The main contact.html form now submits directly to
# HubSpot via its embed and no longer goes through this endpoint.

# Slack Incoming Webhook URL for the #contact-form channel.
# Create at https://api.slack.com/apps -> your app -> Incoming Webhooks.
SLACK_WEBHOOK_URL=

# Resend API key used to email partner-application submissions.
# Create at https://resend.com -> Dashboard -> API Keys.
RESEND_API_KEY=

# Optional. Leave blank in normal use: the code already defaults to
# "4C Website <website@send.4ccs.com>", sent from the send.4ccs.com domain
# verified in Resend. Only set this if that sending address changes.
EMAIL_FROM=
